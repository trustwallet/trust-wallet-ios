// Copyright SIX DAY LLC. All rights reserved.

import UIKit

enum OpenInTrustError: LocalizedError {
    case invalidContext
    case invalidProvider
    case invalidURL
    case loadItemFailed
    case cancel
}
